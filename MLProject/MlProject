name: insurance-training

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_path:
        type: path
        default: namadataset_preprocessing/dataset.csv
    command: >
      python modelling.py --data_path {data_path}